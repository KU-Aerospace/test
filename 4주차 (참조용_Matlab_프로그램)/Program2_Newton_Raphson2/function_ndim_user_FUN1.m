%-------------------------------------------------------------------------
% objective : computing function at a given x-vector
%-------------------------------------------------------------------------
% Input Variables 
%    x      : current values of each variable
%-------------------------------------------------------------------------
% Output Variables 
%    fun    : function values at x
%-------------------------------------------------------------------------
function [fun]=function_ndim_user_FUN1(x)
%-------------------------------------------------------------------------
%  function #1--> solution (xx,yy)= (2, 3)
%-------------------------------------------------------------------------
    xx = x(1,1);
    yy = x(2,1);
    fun(1,1) = xx*xx + xx*yy -10.0;
    fun(2,1) = yy + 3*xx*yy*yy - 57;
%-------------------------------------------------------------------------
%  function #2--> solution (xx,yy)= (1,0), (0,1)
%-------------------------------------------------------------------------
%    xx = x(1,1);
%    yy = x(2,1);
%    fun(1,1) = xx*xx + yy*yy -1.0;
%    fun(2,1) = (xx-1.0)^2+(yy-1.0)^2-1.0;
%-------------------------------------------------------------------------
